{
  "name": "@babycam/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured yet'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@babycam/shared-types": "workspace:*",
    "@babycam/webrtc-config": "workspace:*",
    "@babycam/design-tokens": "workspace:*",
    "socket.io-client": "^4.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
